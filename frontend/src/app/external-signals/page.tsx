import ExternalSignalsFeed from '@/components/ExternalSignalsFeed';

export const metadata = {
  title: 'External Signals Feed | AnvayaAI',
  description: 'Real-world external factors affecting supply chain operations — weather, market trends, and disruption alerts.',
};

export default function ExternalSignalsPage() {
  return <ExternalSignalsFeed />;
}
